function calcAverage(arr) {
  var sum;
  for (var i = 0; i < arr.length; i++) {
    sum += arr[i];
  }
  return sum;
}
calcAverage([85, 90, 92]);

  // 1. lines 2, 6
  // 2. node debugger
  // 3. line 2- sum has to be assigned with a number
// line 6 - sum should be divided by arr.length